<div class="search-items">
    <div class="row">
        {{-- <div class="col-12">
            @foreach ($products as $product)
                <a href="{{ route('product', ['category' => $product->category->slug, 'product' => $product->slug]) }}"
                    class="product_card">
                    <div class="productlis-card">
                        {{ $product->title }}
                    </div>
                </a>
            @endforeach
        </div> --}}
    </div>
</div>
